#include "Question.h"
#include <stdio.h>
#include <string.h>

void find_longest_common_substring(
    const char *str1,
    const char *str2,
    size_t *str1_substring_start_idx,
    size_t *str2_substring_start_idx,
    size_t *substring_length)
{

    int current_idx_str1 = *str1_substring_start_idx;
    int current_idx_str2 = *str2_substring_start_idx;
    
    while(*str1){
        if(*str1==*str2)
        {

        }
    }
}